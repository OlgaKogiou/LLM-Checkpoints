# driver present?
nvidia-smi

# OS deps used by pccheck and common DL tooling
sudo apt-get update
sudo apt-get install -y build-essential git wget curl cmake \
    libpmem-dev librpmem-dev libpmemblk-dev libpmemlog-dev libpmemobj-dev libpmempool-dev \
    libpci-dev numactl


# If you don’t have conda yet:
cd ~
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh
bash miniconda.sh -b -p $HOME/miniconda
eval "$($HOME/miniconda/bin/conda shell.bash hook)"

# fresh env
conda create -y -n pccheck python=3.9
conda activate pccheck

# PyTorch/cu121 wheels
pip install --upgrade pip
pip install --index-url https://download.pytorch.org/whl/cu121 \
  torch torchvision torchaudio


#check the version, GPU checks
python - <<'PY'
import torch
print("CUDA", torch.version.cuda, "| Torch", torch.__version__)
print("GPUs:", torch.cuda.device_count())
PY


git clone https://github.com/eth-easl/pccheck.git

cd pccheck

chmod 777 install_preq_at_vm.sh

chmod 777 install.sh

cd ~/pccheck/artifact_evaluation

chmod 777 test_simple.sh 


 ./test_simple.sh #(vgg dataset, it will make ckpt file on current dir named: pccheck_checkpoint.chk)


 Other model is complaining
